---
version: 2

plan:
  project-key: FAST
  key: FAST
  name: Demo FAST testing plan

stages:
  - building:
      - build
  - testing:
      manual: false
      final: false
      jobs:
        - tests
  - cleaning:
      final: true
      jobs:
        - cleanup

build:
  key: BLD
  variables:
    secret_token: BAMSCRT@0@0@C7rsIm35KmrpZdGnfYTt50PZSa4GpgPdFpr2WAEj4sQ58NihLbP/F8o8K2na9tb/5ngou2tt6KUe+wuQMyPg8IRWw4khzbe4Z5IY7VTtycW/wjmszLfAlGl5Y3ck3Nih5uC6s1VZfXD9nCRV1Sx/dpumIR9sPJep3jrzPxJBz5o=
  tasks:
    - script:
        interpreter: /bin/sh
        scripts:
          - docker-compose build

tests:
  key: TST
  tasks:
    - script:
        interpreter: /bin/sh
        scripts:
          - docker-compose up -d dvwa fast selenium
          - docker-compose run --service-ports test
          - docker-compose run -e CI_MODE=testing -e TEST_RUN_URI=http://dvwa:80 fast

cleanup:
  key: CLN
  tasks:
    - script:
        - docker-compose down
